# elixir_simple_oauth - 交接重点与风险清单

## 1. 高风险点
- 各 provider 错误语义不同，统一错误映射容易遗漏。
- token 缓存逻辑在高并发下可能出现竞态刷新。

## 2. 排查入口
- 对应 provider 模块 + client 模块
- `SimpleOAuth.TokenServer`

## 3. 接手建议
1. 为每个 provider 保留一组可回归的 sandbox 凭证。
2. 在上层应用增加失败重试与降级策略。
