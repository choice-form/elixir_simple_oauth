# elixir_simple_oauth - 数据模型与存储

## 1. 存储形态
- 无数据库。
- token 缓存在 GenServer 内存结构中。

## 2. 关键结构
- `TokenServer.Record`：provider/key/value/expires_in/updated_at/fetcher。
- `TokenServer.Context` / `IsolateContext`：记录管理策略。

## 3. 一致性关注
- 多实例部署时需评估内存缓存一致性。
- token 过期边界与系统时钟漂移需注意。
